# XIV 地牢

FFXIV 主题的像素地牢风格回合制 DRPG 原型，使用 Vite、TypeScript、PixiJS 和 rot.js 构建。

## 本地运行

```bash
npm install
npm run dev
```

## 验证

```bash
npm test
npm run build
```

推送到 `main` 分支后，GitHub Actions 会自动构建并部署到 GitHub Pages。

